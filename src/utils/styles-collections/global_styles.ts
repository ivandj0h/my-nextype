const mainStyle = ["flex"].join(" ");
const container = ["container", "mx-auto", "text-center"].join(" ");
const textLg = ["text-lg", "font-bold", "text-gray-300"].join(" ");

export {
    mainStyle,
    container,
    textLg
}
